# BACKEND

To create the test file with the specified content, you can use the following command in your terminal:

```bash
echo "SFTP file creation works" > /home/marshall/projects/test-sftp.txt
```

This command will create a file named `test-sftp.txt` in the `/home/marshall/projects` directory with the content "SFTP file creation works". 

If you encounter any issues, please let me know!